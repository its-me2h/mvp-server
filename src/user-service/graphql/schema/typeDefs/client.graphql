type Client {
    id: ID
    userId: String
    complexId: String
    blockId: String
    apartmentId: String
}

input CreateClientInput {
    id: ID!
    userId: String!
    complexId: String!
    blockId: String!
    apartmentId: String!
}
